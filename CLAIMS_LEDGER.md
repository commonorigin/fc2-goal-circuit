# Paper C — Claims Ledger

> Source of truth for every number in `main.tex`. Each claim → its artifact. Do not put a number
> in the paper that is not traceable here. Status legend:
> **[REPRO]** reproduced from committed code/test (re-runnable); **[DATA]** direct from the committed
> connectome data; **[LIT]** published literature (deep-research verified); **[ANALOGY]** a
> cross-system analogy stated as prediction; **[DECLARED]** a declared modelling assumption;
> **[TO-ATTACH]** stated in the project record but the standalone artifact must be attached/re-run
> before submission.
>
> **Test gates:** one committed gate per claim in `tests/our_ai/test_fc2_*.py` (re-run in CI; all
> pass). The availability statement references this suite without pinning a brittle integer, since the
> gate count grows as claims are added.

## §2 Connectome structure

| # | Claim | Value | Artifact | Status |
|---|-------|-------|----------|--------|
| C1 | FB5A → all FC2 | 4 FB5A cells → all 85 FC2, 7,741 synapses (transmitter predicted GABAergic — see C17b, NOT measured) | FlyWire feather (`proofread_connections_783.feather`) via `experiments/our_ai/connectome_steering/data/build_fc2_selection.py` (selects FC2A/B/C + FB5A) | [DATA] |
| C2 | FB5A inhibition uniform across bearing | 4 FB5A → **85/85** FC2; per-cell inh_w bearing-uncorrelated (corr cos/sin = **+0.02 / −0.04**); disynaptic FC2→FB5A→FC2 loop flat (**modulation 5%**) | `experiments/our_ai/connectome_steering/data/analyze_fc2_scoping.py` → `fc2_scoping.json` (+ fig `c2_fb5a_uniform_vs_bearing.png`); gate `tests/our_ai/test_fc2_scoping.py::test_c2_fb5a_inhibition_is_uniform_across_bearing` | [REPRO] |
| C3 | No local recurrent excitation | FC2→FC2 negligible (off-diag mean **0.12**); FC2→hΔ→FC2 anti-local (**near 37.8 → far 94.7, 2.5×**); vΔ weak (**5%** of hΔ) | `analyze_fc2_scoping.py` → `fc2_scoping.json` (+ fig `c3_recurrence_antilocal.png`); gate `test_fc2_scoping.py::test_c3_no_direct_fc2_recurrence` / `::test_c3_hdelta_recurrence_is_antilocal` / `::test_c3_vdelta_recurrence_is_weak` | [REPRO] |
| C2b/C3b | **Cross-connectome replication (2nd brain)** | Hemibrain (Scheffer 2020) independently reproduces C2+C3: FB5A **88/88**, disynaptic loop flat (**modulation 1.2%**), FC2→FC2 negligible (0.29), FC2→hΔ→FC2 anti-local (**ratio 2.43** vs FlyWire 2.50; full precision **2.429** hemibrain vs **2.504** FlyWire, a **3%** difference), vΔ weak (5%). **Caveat (gated):** the raw per-cell FB5A weight carries a partial-volume completeness gradient (per-column FB5A vs total-synapses corr **0.93**), so the C2 direct check uses the completeness-normalized weight (raw sin-corr 0.35 → 0.145). Bearing = the real FB column index (`_C<k>_`). | `analyze_fc2_scoping_hemibrain.py` → `fc2_scoping_hemibrain.json` (+ fig `w3_flywire_vs_hemibrain.png`); gate `tests/our_ai/test_fc2_scoping_hemibrain.py` (5/5) | [REPRO] |
| C7a | **FC2 upstream input map (the distributed goal drive)** | Measured, 100% typed: FB-tangential **55%** (broad gating), hΔ/vΔ recurrence **37%**, PFN **5%**, MBON **0% direct** (96 MBONs in the brain → MB influence indirect; refines the prior expectation of "MB valence upstream (direct)"). | `experiments/our_ai/connectome_steering/data/trace_fc2_inputs.py` → `fc2_inputs.json` (+ fig `w4_fc2_input_map.png`); gate `tests/our_ai/test_fc2_inputs.py::test_fb_tangential_dominates_input` / `::test_mbon_is_not_a_direct_input` | [REPRO] |
| C7b | **Directional carrier vs normalizer (per-cell)** | At single-cell resolution the two roles separate cleanly: **PFN** cells each target a NARROW bearing band (~10/85 FC2, conc **0.96**) = the directional feedforward drive; **FB5A** blankets **84/85** FC2 bearing-flat (conc **0.21**) = the global normalizer (confirms C2 per-cell). The whole FB-tangential family is broad; hΔ/vΔ are narrow but recurrent (anti-local, C3). | `trace_fc2_inputs.py`; gate `test_fc2_inputs.py::test_pfn_are_narrow_directional_carriers` / `::test_fb5a_is_a_broad_normalizer` / `::test_directional_vs_normalizer_separation` | [REPRO] |
| C7e | **Upstream goal-substrate nomination + Lanz hΔK–PFG exclusion (§4b, the novelty)** | FC2's directional input = recurrent **hΔ network ~28%** (hΔJ 6.14%, hΔC 3.65%; per-cell conc hΔC **0.91**) vs broad non-directional FB-tangential **54.5%** (conc **0.21**), PFN 5.3% (conc 0.96). **DECISIVE EXCLUSION:** the Lanz 2025 hΔK–PFG persistent-goal attractor supplies **<0.2%** of FC2 input (**hΔK 0.069% = 54 syn, PFGs 0.113% = 88 syn** of 78,193) → PFL-facing, not FC2's source. **Valence route:** MBON→FC2 **0%** direct; MBON→FB5AB **5.11%** → FB5AB→hΔC **6.92%** (top named input) + FB5AB→FC2 **1.74%**. hΔ within-class recurrence **9.75%** (`hdelta_within_class_recurrence_frac`). hΔK **output** is PFL/FB6-facing (`hdeltaK_output_top_targets`: PFGs 40.9%, FB6A 17.8%, ExR3 7.1%) and only **0.36%** to FC2 (`hdeltaK_output_to_fc2_frac`). Nomination (not proof): hΔC-led network holds the goal, PFN injects bearing, FB5AB gates valence. Falsifiable double-dissociation. | `analyze_fc2_selector.py` → `fc2_selector.json` (+ fig `csel_fc2_selector.png`); gate `tests/our_ai/test_fc2_selector.py` (3/3) | [REPRO] |
| C3c | **Decomposition sum (§2)** | On the common disynaptic scale, total FC2→FC2 inhibition = FB5A **~95% of mass, flat** (far/near 1.0) + hΔ **~5% of mass but ~85% of cross-bin variance / all the distance-dependence** (far/near 2.5). Predicts the reported sign (rises with distance) as a modest anti-local rise on a uniform floor, NOT a steep gradient; a normalizer subtracts the FB5A floor, leaving hΔ functionally. Caveat: synapse-count proxy; shape vs their reported sign, not a fit to their (unavailable) digitized curve. | `analyze_fc2_scoping.py` → `fc2_scoping.json` (profiles) | [REPRO] |
| C7d | **Distributed uniform-inhibition substrate (transmitter-robust normalizer)** | Of FC2's uniform inputs (types reaching ≥50% of FC2): **FB5A is the largest single one** (7,741 syn, 85/85) and the **only GABA-predicted** one, but just **~12%** of the uniform-input mass. The rest is a broad **glutamatergic** FB-tangential family (**40 types**; glutamate is inhibitory via GluClα; FB5AA among the siblings, **predicted-glutamatergic**; glutamate ≈ **39%** of the uniform-input mass — the largest inhibitory-capable leg). Uniform-input mass: **~51% inhibitory-capable** (GABA+glut) / **~49% cholinergic** (excit). GABAergic base rate among experimentally-typed FB-tangential cells: **14 GABAergic vs 371 GABA-negative** (`fb_tangential_known_gaba` / `_known_gaba_negative`, from `known_nt`). ⇒ the normalizer computation is **distributed and robust to FB5A's transmitter** (GABA *or* glutamate both inhibit); **honest caveat:** net uniform sign not resolvable from wiring (needs FC2 receptors). | `analyze_fc2_uniform_inhibitors.py` → `fc2_uniform_inhibitors.json` (+ fig `cui_fc2_uniform_inhibitors.png`); gate `tests/our_ai/test_fc2_uniform_inhibitors.py` (4/4) | [REPRO] |
| C7c | **No single localized goal-setter** | Type-summed input concentration is ~flat (~0.2) for ALL types incl. PFN — no single stream is a localized goal-setter at population level; the directional signal is carried per-cell (PFN columnar phase) and the drive is distributed → consistent with distributed selection, NOT a WTA hand-off. | `trace_fc2_inputs.py` (per-type vs per-cell) | [REPRO] · honest limit: which input carries the *committed* goal is a reasoned nomination, not proof |

## §3 The tested negative (no within-FC2 WTA)

| # | Claim | Value | Artifact | Status |
|---|-------|-------|----------|--------|
| C4c | **Committed LIF spiking model (turns "spiking would fail" into a result)** | A real leaky integrate-and-fire FC2 network (85 neurons, threshold/reset/leak) with FB5A global-inhibition feedback, under the seeded-basin test: basin gap **0.00°**, mean settled rate 2.56 (non-degenerate). Spike timing does not rescue a WTA — confirms the rate result from an actual spiking model. | `analyze_fc2_lif.py` → `fc2_lif.json`; gate `tests/our_ai/test_fc2_lif.py` (2/2) | [REPRO] |
| C4b | **vΔ tested as local excitation, gain-swept (hardens "no local excitation")** | The one LOCAL recurrent route (vΔ, decays with distance, undetermined sign) run as local EXCITATION (the ring-attractor ingredient) and **swept over the same gain range as hΔ (2–10)**: basin gap **≤2°, never crosses the 30° bound at any gain** (`vdelta_basin_gap_vs_gain`, `vdelta_latch_crossing_gain=None`), unlike hΔ (crosses at ~6). So "no local excitation" is gain-robust, not reference-only. So "no local recurrent excitation" is a TESTED result, not a sign assumption. | `prove_fc2_no_wta.py` (`real_vdelta_local_excit`, matrix from `fc2_recurrence.json`); gate `test_fc2_no_wta.py::test_c4_connectome_families_do_not_commit[real_vdelta_local_excit]` | [REPRO] |
| C4 | Tested 5 mechanism families | subtractive / divisive / divisive+noise (spiking-proxy, Option A) / **hΔ anti-local matrix**; none is bistable. basin_gap: subtractive/divisive/global-only ≈ **0°** (literally 1e-14); **hΔ anti-local = 24.4°** (below the 30° no-latch bound, NOT ≈0°). **Gain-contingent:** the hΔ coupling matrix is connectome-pinned but its scalar gain (`goal_nowta_hdelta_gain=3.0`) is anatomically unpinned; the hΔ family **latches above gain ≈6** (31.8° at 6, 38.4° at 8 — `hdelta_basin_gap_vs_gain`, `hdelta_latch_crossing_gain=6.0`). So the hΔ no-latch conclusion holds only at the reference gain; the pure-global families do not latch at any gain, and the structural no-local-excitation (C3) + feedforward (C6b) results do not depend on it. | `prove_fc2_no_wta.py` → `fc2_no_wta.json` (+ fig `c4c5_no_wta_bistability.png`); gate `tests/our_ai/test_fc2_no_wta.py::test_c4_connectome_families_do_not_commit` | [REPRO] |
| C5 | Divisive norm shifts but does not commit | **basin_gap ≈ 0° at 0/10/20% asymmetry** (init-invariant → no WTA even at 20%) | `prove_fc2_no_wta.py`; gate `test_fc2_no_wta.py::test_c5_divisive_no_commit_across_asymmetry` | [REPRO] |
| C6 | Structural cause | global inhibition + no local excitation ⇒ normalizer, not WTA. **Controls:** uniform ring + local excitation → **basin_gap 114°** (bistable WTA, detector validated); same ring global-only → 0° (isolates local excitation as the missing ingredient) | `prove_fc2_no_wta.py`; gate `test_fc2_no_wta.py::test_positive_control_is_bistable_detector_valid` / `::test_no_wta_conclusion_needs_local_excitation` | [REPRO] |
| C6c | **Real-bearings positive control + chosen bounds (traceability)** | Beyond the idealized-ring 114° control (C6), local excitation injected on the **real irregular FC2 bearings** still latches at **78.2°** (`ctrl_pos_local_excit_realpsi`), above the **60° WTA bound** — the detector catches a WTA on the real geometry too, so the negative results are not a sampling artifact (the 78° vs 114° gap shows real sampling is somewhat conservative). Bounds are **chosen** thresholds: no-latch **30°**, WTA **60°** (`thresholds.max_gap_deg` / `min_wta_gap_deg`). The hΔ gain sweep is **non-monotonic** past its peak (38.4° at gain 8 → **32° at gain 10**), never reaching 60°. | `prove_fc2_no_wta.py` → `fc2_no_wta.json`; gate `tests/our_ai/test_fc2_no_wta.py` (real-ψ positive-control assertion) | [REPRO] |
| C6b | **No FEEDFORWARD max-selection either** | Sweeping the sharpening exponent p: at the operating point **p=2** two competing goals are **co-represented** (equal→loser keeps 45% of mass; stronger goal correctly leads 60/40). Collapse to one column only at **p≳12** (6× the operating point) and it is **init-INDEPENDENT** (seedA-vs-seedB winner spread **0.00%** → a fixed anatomical bias, NOT a bistable choice) and does **not track the goal** (stronger input *loses*, 46% at p=30). ⇒ no goal-WTA at any exponent, confirming the bistability result from an independent axis. | `analyze_fc2_feedforward.py` → `fc2_feedforward.json` (+ fig `cff_feedforward_no_wta.png`); gate `tests/our_ai/test_fc2_feedforward.py` (4/4) | [REPRO] |

## §5b Prediction robustness — headline = the JOINT two-axis experiment (§7): PRIMARY locality axis (competition-preservation, C22) + amplitude axis (disinhibition, C21). NOT the mush/shape effect.

| # | Claim | Value | Artifact | Status |
|---|-------|-------|----------|--------|
| C20 | Shape/mush effect is FRAGILE | "FB5A off → more bumps" holds in only **30%** of a 3×3×3 gain sweep (multipliers on the committed operating point: power ×{0.7,1.0,1.4}, g_inh ×{0.5,1,2}, σ ×{0.5,1,2}) — a tuned-operating-point artifact (uniform inhibition, C2, cannot reshape a bump) | `analyze_fc2_prediction.py` → `fc2_prediction.json`; gate `test_fc2_prediction.py::test_mush_shape_effect_is_fragile` | [REPRO] |
| C21 | Disinhibition effect is ROBUST (secondary prediction) | "FB5A off → total FC2 activity rises" holds in **100%** of gains (amp off/on median 39×); bump shape preserved (median Δconc 0.028; single goal: concentration **0.86 on → 0.85 off**). Directional claim (magnitude=artifact). SECONDARY signature — depends on FB5A being inhibitory. | `analyze_fc2_prediction.py`; gate `test_fc2_prediction.py::test_disinhibition_is_robust` / `::test_bump_shape_preserved_under_silencing` | [REPRO] |
| C22 | **Competition structure preserved (PRIMARY prediction: uniform-vs-local, form/sign-robust)** | Under two competing goals (drive bumps **120° apart, amplitude ratio 1:0.7**), silencing FB5A PRESERVES the relative activation ratio of the competing columns for ANY spatially-uniform form — **divisive 0.003 / subtractive 0.001 / excitatory 0.008** (all \|log-dev\|<0.01) — vs a local-selector control at **~21** (>2000× separation). Robust to inhibition non-uniformity up to **α≈0.3–0.4** (logdev crosses the 0.1 bound between the α=0.3 and α=0.5 grid points; the JSON `nonuniformity_break_alpha=0.5` is the first *breaking* sample); measured inh_w non-uniformity **0.04** (safe under either reading). **Scope-corrected:** the discriminator is UNIFORM-vs-LOCAL (depends on C2, MEASURED — not on transmitter/sign/form); preservation RULES OUT a within-FC2 local selector but does NOT alone prove the normalizer. (Dropped the earlier "cancels in the ratio → transmitter-independent" over-claim, exact only for the divisive form; the empirical form-sweep is the honest justification.) **Headline prediction.** | `analyze_fc2_competition.py` → `fc2_competition.json` (+ fig); gate `tests/our_ai/test_fc2_competition.py` (3/3) | [REPRO] |

## §5 In-silico demonstration (E.FC2 — REPRODUCED)

| # | Claim | Value | Artifact | Status |
|---|-------|-------|----------|--------|
| C7 | Model form | Eq. (1) divisive normalization; real FB5A→FC2 `inh_w` + FC2→FB5A `pool`; gains `power/g_inh/sigma` task-agnostic | `experiments/our_ai/fc2_selection.py::GlobalInhibitionNormalizer`; cache `.../data/fc2_selection.json` | [REPRO] |
| C8 | Single goal → one clean bump | resultant concentration **0.86** (per-step-normalized form, the shipped `GlobalInhibitionNormalizer`); the printed Eq. (1) iterated **raw** gives **0.88** — same clean-single-bump conclusion, scale-free. | `tests/our_ai/test_fc2_selection.py::test_single_goal_is_one_clean_bump` (asserts `conc > 0.6`) | [REPRO] |
| C9 | Prediction in silico | FB5A ON → 1 dominant bump (conc 0.57); silenced (`g_inh=0`) → 2 bumps (conc 0.53) — a **modest, graded** re-emergence, NOT a collapse (uniform inhibition rescales, doesn't reshape; the power term does the sharpening) | `test_fc2_selection.py::test_fb5a_enforces_single_bump_prediction` | [REPRO] |
| C10 | Cache = real FB5A weights | `inh_w`/`pool` shape (85,), FB5A reaches every FC2 (85/85) | `test_fc2_selection.py::test_cache_is_real_fb5a_weights` | [REPRO] |
| C11 | Reusable = APL motif | same `GlobalInhibitionNormalizer` serves MB/APL sparsification | `fc2_selection.py` docstring | [REPRO/design] |

## §6 Behaviour anchors (LITERATURE — deep-research verified)

| # | Claim | Value | Source | Status |
|---|-------|-------|--------|--------|
| C12 | FC2 bump tracks goal | r = **0.61** (EPG heading bump 0.88) | Mussells-Pires 2024 | [LIT] |
| C13 | Rotation-invariant set-point | phase does not deviate under ±90° virtual rotation (V-test **P=6.65e-4**) | Mussells-Pires 2024 (deep-research verified) | [LIT] |
| C14 | Commits, not averages | two bumps 180° apart → two headings **~166°** apart | Mussells-Pires 2024 | [LIT] |
| C15 | Multi-stability | single-pattern stabilization vs averaging under ambiguous motion | **Toepfer, Wolf & Heisenberg 2018, PLOS Biology 16(2):e2003113** (verified; concerns orientation behaviour) — an ANALOGY, not a direct FC2 fit | [ANALOGY] |

## §7–§8 Prediction + declared limits

| # | Claim | Status |
|---|-------|--------|
| C16 | Prediction: silence FB5A → **FC2 DISINHIBITS (activity rises), goal bump tuning PRESERVED** — not reshaped, not abolished | the **amplitude axis** of the §7 joint experiment (the *mechanism* test; SECONDARY to the PRIMARY locality axis C22) — **prediction, not confirmation** (no recordings). Note: the activity-*rises* direction is definitional (g_inh=0 removes a denominator term); the measured content is tuning-PRESERVED-while-rising. the earlier "mush / modest broadening" (shape) prediction is FRAGILE (holds in 30% of a gain sweep — a threshold artifact, since uniform inhibition C2 can't reshape). The robust prediction is DISINHIBITION-without-reshaping (amplitude rise robust in 100% of gains; direction only, magnitude=artifact) — the true normalizer signature vs a spatial selector's goal-selection loss. See C20/C21. |
| C17 | FB5A inhibition is *divisive* (shunting) | **[DECLARED]** modelling assumption (receptor property the connectome does not resolve; the no-WTA negative holds for the subtractive form too) |
| C17b | FB5A is GABAergic (inhibitory) | **[PREDICTED]** FlyWire/Eckstein classifier `top_nt`=gaba, **conf ~0.79**; `known_nt`=empty (no experimental verification). The structural no-WTA result (C2/C3/C4-C6) uses only connectivity and holds regardless of transmitter; only FB5A's inhibitory *role* rests on the prediction. |
| C18 | Distributed selection = partial answer | states where the selector is NOT (inside FC2) + names FB5A's role; the upstream drive is now MEASURED (W4: FB-tangential 55%, PFN directional, MBON indirect) but which input carries the *committed* goal is a reasoned nomination, not proof. Credits Hulse 2021 + Matheson 2022. |
| C19 | APL analogy locality | the FB5A≡APL identity is the **normalization computation**; anatomically APL inhibition is spatially graded, not literally global (`amin2020`) — qualified in main.tex §4. |
